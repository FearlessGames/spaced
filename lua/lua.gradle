apply plugin: 'base'

configurations {
	common
}

version = 1.0

task packageLua(type: Zip) {
	from 'src/main/lua'
	baseName = 'lua-common'
}

assemble.dependsOn packageLua

artifacts {
	common packageLua
}