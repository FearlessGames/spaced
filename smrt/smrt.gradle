apply plugin: 'base'

configurations {
	templates
}

version = 1.0

task packageTemplates(type: Zip) {
	from 'src/main/resources'
	baseName = 'smrt-templates'
}

assemble.dependsOn packageTemplates

artifacts {
	templates packageTemplates
}